/** @type {import('tailwindcss').Config} */
export default {
	// For Tailwind CSS v4, the config is now handled via CSS
	// This file exists for IDE support and legacy compatibility
	content: ['./src/**/*.{html,js,svelte,ts}'],
	theme: {
		extend: {}
	},
	plugins: []
};
